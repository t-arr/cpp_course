#pragma once

#include <string>

#include "test_config.hpp"

const std::string TEST_DATA_OUTPUT_IMAGE_FOLDER = std::string(TEST_IMAGE_OUTPUT_FOLDER);
const std::string TEST_DATA_INPUT_IMAGE_FOLDER = std::string(TEST_IMAGE_INPUT_FOLDER);
